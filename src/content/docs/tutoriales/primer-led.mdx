---
title: 'Tutorial — Primer LED'
description: 'Aprende a encender un LED en RB0: proyecto MPLAB, CONFIG, TRISB y LATB paso a paso.'
---

import PracticeLabPage from '../../../components/PracticeLabPage.astro';
import PracticeBox from '../../../components/PracticeBox.astro';
import AIExplain from '../../../components/AIExplain.astro';
import Figure from '../../../components/Figure.astro';

<PracticeLabPage tutorialSlug="primer-led">

<PracticeBox title="Encender un LED en RB0" level="Inicial">
Objetivo: configurar `RB0` como salida y escribir un nivel alto para encender un LED conectado correctamente con su resistencia.
</PracticeBox>

<Figure
	src="/images/gpio/led-rb0.svg"
	alt="Esquema electrónico de LED en RB0 con resistencia de 330 ohm"
	caption="Montaje básico: RB0 como salida, resistencia 220-330 ohm y LED a tierra."
	credit="Práctica 1 LED — UNEXPO"
/>

<AIExplain title="Concepto antes del código">
`TRISB` decide si cada pin del puerto B es entrada o salida. Un bit en `1` significa entrada; un bit en `0` significa salida. Para usar `RB0` como salida, el bit 0 de `TRISB` debe estar en `0`.
</AIExplain>

## Código completo de referencia

```asm
        LIST    P=18F4550
        #include <P18F4550.INC>

        CONFIG  FOSC = HS
        CONFIG  WDT = OFF
        CONFIG  LVP = OFF

        ORG     0x0000
        GOTO    inicio

inicio:
        CLRF    LATB        ; Limpia salidas del puerto B
        BCF     TRISB, 0    ; RB0 como salida
        BSF     LATB, 0     ; RB0 en alto

bucle:
        GOTO    bucle

        END
```

## Qué debes poder explicar

1. Por qué se usa `TRISB`.
2. Por qué `BCF TRISB, 0` convierte `RB0` en salida.
3. Por qué se escribe en `LATB` y no necesariamente en `PORTB`.
4. Por qué el programa termina en un bucle infinito.

## Ejercicio

Modifica el programa para que el LED esté en `RB1`. Luego explica qué líneas cambiaron y por qué.

</PracticeLabPage>

[Panel de tutoriales](/tutoriales/) · [Ejercicios](/tutoriales/ejercicios/)
