---
import PracticeTutorialFlow from './PracticeTutorialFlow.astro';

interface Props {
	slug: string;
	locale?: 'es' | 'en';
}

const { slug, locale = 'es' } = Astro.props;
---

<PracticeTutorialFlow slug={slug} locale={locale} />
